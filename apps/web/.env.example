# Copy to .env.local for local development, or set at build time in CI.
#
# Local:    the Fastify service on its default port
# Deployed: the Cloud Run URL, e.g. https://simulator-xxxxxxxx-as.a.run.app
VITE_SIMULATOR_URL=http://localhost:8080
