/**
 * Bump PRESET_VERSION whenever any preset or statutory constant changes.
 * It is stamped into every shared scenario URL so a link opened months later
 * either reproduces its original numbers or tells the reader they have moved.
 */
export const PRESET_VERSION = "2026.08.1";

/** Bump on any change to the maths itself. Parity and share-URL replay depend on it. */
export const ENGINE_VERSION = "0.1.0";
