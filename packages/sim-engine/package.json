{
  "name": "@spotential/sim-engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The single implementation of the What-if Simulator maths. Pure, zero runtime dependencies, consumed as TypeScript source by both the browser and Cloud Run so client and server cannot drift.",
  "exports": {
    ".": "./src/index.ts",
    "./presets": "./src/presets/index.ts"
  },
  "sideEffects": false,
  "scripts": {
    "typecheck": "tsc --noEmit"
  }
}
